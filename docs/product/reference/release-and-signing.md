# Release and signing

This section points to the release-specific docs that already exist in the repo.

## Release process

Use the project release process doc for:

- conventional commit expectations
- local hooks
- CI gates
- desktop verification
- release-please flow
- manual rebuild notes

See [Release process](../../release-process.md).

## macOS signing

Use the signing doc for:

- required GitHub secrets
- certificate export steps
- notarization setup
- notes about Electron Builder signing behavior

See [macOS signing](../../macos-signing.md).

## Desktop smoke checks

Use the platform smoke checklist when validating release builds across desktop targets.

See [Desktop platform smoke checklist](../../desktop-platform-smoke-checklist.md).

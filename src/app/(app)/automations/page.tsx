import { PageWrapper } from "@/components/shell";

export default function AutomationsPage() {
  return (
    <PageWrapper
      subtitle="Automation management UI will be layered into the app shell next."
      title="Automations"
    />
  );
}
